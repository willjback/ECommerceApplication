<UserControl x:Class="ECommerceApplication.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ECommerceApplication.Views"
             xmlns:dialog="clr-namespace:ECommerceApplication.Dialog"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" FontFamily="Segoe UI Light" FontSize="20">

    <Grid Background="GhostWhite">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="gridProducts" Grid.Row="0"/>

        <Grid Grid.Row="1" Margin="0">
            <Button x:Name="BtnSelect" Content="Select" Height="30" Width="85" Click="BtnSelect_Click"/>
        </Grid>

    </Grid>
</UserControl>
