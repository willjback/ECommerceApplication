<UserControl x:Class="ECommerceApplication.Views.CartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ECommerceApplication.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" FontFamily="Segoe UI Light" FontSize="20" IsVisibleChanged="UserControl_IsVisibleChanged">
    <Grid Background="AliceBlue">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="gridCart" Grid.Row="0" AutoGeneratingColumn="gridCart_AutoGeneratingColumn"/>

        <Grid Grid.Row="1">
            <Button x:Name="BtnCheckout" Content="Checkout" Height="30" Width="85" Click="BtnCheckout_Click"/>
        </Grid>
    </Grid>
</UserControl>
